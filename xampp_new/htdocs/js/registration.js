function show_item(element)
{
   var stuff = document.getElementById(element);
   stuff.style.display = "block";
}

function hide_item(element)
{
   var stuff = document.getElementById(element);
   stuff.style.display = "none";
}